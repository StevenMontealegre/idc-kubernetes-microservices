apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgsql12-idc-inscripcion
  labels:
    app: db-inscripciones
    type: db
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: db-inscripciones
      type: db
  template:
    metadata:
      labels:
        app: db-inscripciones
        type: db
    spec:
      containers:
        - name: pgsql12-idc-inscripcion
          image: postgres:latest
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: db_idc_inscripcion
            - name: POSTGRES_PASSWORD
              value: Carvajal2019*

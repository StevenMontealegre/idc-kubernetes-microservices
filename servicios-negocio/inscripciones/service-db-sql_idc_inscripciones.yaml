apiVersion: v1
kind: Service
metadata:
  name: sqlserver-idc-inscripcion
  labels:
     app: db-inscripciones
spec:
  type: NodePort
  ports:
  - port: 1433
    nodePort: 30061
    protocol: TCP
  selector:
     app: db-inscripciones

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgsql-idc-formularios
  labels:
    app: db-formularios
    type: db
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: db-formularios
      type: db
  template:
    metadata:
      labels:
        app: db-formularios
        type: db
    spec:
      containers:
        - name: pgsql-idc-formularios
          image: postgres:latest
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: db_idc_formularios
            - name: POSTGRES_PASSWORD
              value: Carvajal2019*
